VITE_API_URL=http://localhost:5000

# Production: set in the Vercel project (not in git). Rebuild after changing.
# VITE_API_URL=https://api.phoenixadventures.in
# (or https://phoenix-api.onrender.com until the Hostinger api CNAME is live)
# Replace frontend/public/upi-merchant-qr.png with the real merchant QR before launch.
