VITE_API_URL=http://localhost:5000

# Public Razorpay Key ID (safe to expose in the browser bundle).
# Get it from https://dashboard.razorpay.com/app/keys
# Use a Test key (rzp_test_*) for development, Live key (rzp_live_*) for production.
VITE_RAZORPAY_KEY_ID=rzp_test_replace_me
